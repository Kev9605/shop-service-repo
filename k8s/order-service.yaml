apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: shop-app
spec:
  type: NodePort
  selector:
    app: order-service
    version: blue
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30007